{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Application Configuration",
  "description": "Configuration schema for application-level settings",
  "type": "object",
  "properties": {
    "debug": {
      "type": "boolean",
      "default": false,
      "description": "Enable debug mode for detailed logging and diagnostics."
    },
    "log_level": {
      "type": "string",
      "pattern": "^(DEBUG|INFO|WARNING|ERROR|CRITICAL)$",
      "default": "WARNING",
      "description": "Logging level. DEBUG shows all messages, CRITICAL shows only critical errors."
    },
    "output_file": {
      "type": ["string", "null"],
      "default": null,
      "description": "Path to output file for decoded text. Leave null to print to console."
    },
    "real_time": {
      "type": "boolean",
      "default": false,
      "description": "Enable real-time processing mode for live audio input."
    }
  },
  "additionalProperties": false
}